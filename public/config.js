/*
 * CONFIGURAÇÃO DO FRONTEND
 * ========================
 *
 * String vazia significa "use a API na mesma origem".
 * Em produção, o próprio Worker atende /api/*.
 */

/*
 * URL opcional do backend público.
 *
 * Deixe vazio para usar o backend da mesma origem durante desenvolvimento
 * local e o modo demonstração no GitHub Pages.
 *
 * Exemplo:
 * window.MANAPONTE_API_BASE = "https://api.exemplo.com";
 */
window.MANAPONTE_API_BASE = "";
